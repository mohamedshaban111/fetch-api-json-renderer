# etch-api-json-renderer
A frontend project built with vanilla JavaScript that demonstrates how to fetch data from APIs (GitHub API / local JSON) using Fetch API, process JSON responses, and dynamically render them into a responsive card-based UI.
